# Test dependencies — installs faster than requirements.txt on CI
# Excludes: torchaudio, bitsandbytes, wandb, uvicorn, datasets,
#           accelerate, huggingface_hub, pandas (not needed by tests)

torch>=2.0.0 --index-url https://download.pytorch.org/whl/cpu
transformers>=4.40.0
peft>=0.10.0
jiwer>=3.0.0
librosa>=0.10.0
soundfile>=0.12.0
fastapi>=0.110.0
langdetect>=1.0.9
numpy>=1.24.0
pyyaml>=6.0
evaluate>=0.4.0
python-dotenv>=1.0.0
scikit-learn>=1.4.0
httpx>=0.27.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
