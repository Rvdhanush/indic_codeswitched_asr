# Kept as a stable entrypoint (the Colab notebook and older docs reference it).
# Real dependency sets live in requirements/ and pyproject.toml:
#   requirements/base.txt   core runtime
#   requirements/serve.txt  API / demo on CPU
#   requirements/train.txt  fine-tuning on CUDA   <- this file
#   requirements/dev.txt    tests
-r requirements/train.txt
